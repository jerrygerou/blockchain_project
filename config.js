const DIFFICULTY = 3;
const MINE_RATE = 3000;
// Note to self: is this assuming that the difficulty is directly related to miliseconds?
// Or is the 3 to 3000 simply a coincidence?

const INITIAL_BALANCE = 500;
const MINING_REWARD = 50;

module.exports = { DIFFICULTY, MINE_RATE, INITIAL_BALANCE, MINING_REWARD };
